# 独立客户端

根据 Windows、macOS 或 Linux 平台下载 `mirrorproxy-client-*`。客户端可以查看、
设置、恢复和列出常见包管理器的软件源，保留精确回滚状态，并支持 GitHub HTTPS URL
重写。

使用 `mirrorproxy sources --help` 查看改源命令。选择 MirrorProxy 提供方时必须传入
`--base-url https://你的镜像域名`。系统级改源需要平台正常的管理员权限，执行前可在
Web 门户预览生成的命令。

[English](Client)
